# モバイル表示・UI/UX監査報告

実施日: 2026-07-10  
対象: http://localhost:3000  
主ビューポート: 375×812  
補助ビューポート: 768pxは、既存サーバーが途中で応答しなくなったため未実施  
実施方法: ブラウザで画面遷移・タップ・入力・送信を実行し、画面表示、ローディング、コンソール、ネットワーク失敗を確認。ソースは該当箇所の特定にのみ使用。

## サマリ

再現できた問題は **8件**（P1: 0件、P2: 3件、P3: 5件）です。実機操作中に確認できた画面では、コンソールエラーおよび4xx/5xxのリソースエラーは確認されませんでした。監査途中でブラウザとNodeの双方から localhost:3000 が応答しなくなり、残りの画面と768px確認は未確認です。サーバーを再起動することは禁止されているため、推測で補完していません。

特に優先して直すべきTop 5:

1. P2: ドロワーを開いても背面ページがスクロールする
2. P2: モバイルで投稿種別・チャットモードのラベルが消え、操作内容を判別できない
3. P2: 知識マップ初期状態のノードが小さすぎ、表示ラベルをタップしても詳細が開かない
4. P3: 学習カード生成にキャンセル手段がない
5. P3: 学習カード保存後も同じ保存ボタンが再表示され、保存済み状態が明確でない

## 問題一覧

### [P2] ドロワー表示中も背面ページがスクロールする
- 画面: /
- 再現手順: 1. 375×812でホームを開く → 2. 右上メニューをタップ → 3. ドロワーの背景または背面相当の領域を上下にスワイプする
- 期待する挙動 / 実際の挙動: 期待はドロワー表示中に背面を固定し、閉じた後も元のスクロール位置を保つこと。実際はドロワーを開いたまま document.scrollTop が0から522まで変化し、背面ページが移動した。
- 該当コード（分かれば）: src/components/layout/MobileNav.tsx:97
- 分類: バグ

### [P2] モバイルで投稿種別・チャットモードのラベルが消えて判別できない
- 画面: /posts/new、/chat
- 再現手順: 1. 375×812で /posts/new を開く → 2. 投稿種別タブを確認 → 3. /chatへ移動してモードタブを確認する
- 期待する挙動 / 実際の挙動: 期待は、テキスト・記事URL・YouTube・音声メモ・画像解析、または質問・検索・リサーチ・アカウント分析の違いを画面上で判別できること。実際はモバイル幅でラベルが非表示になり、アイコンだけのタブになった。DOMスナップショットでも対象ボタンのアクセシブルな名前が空だった。
- 該当コード（分かれば）: src/components/ui/SegmentedControl.tsx:21、src/components/ui/SegmentedControl.tsx:65、src/app/posts/new/page.tsx:288、src/app/chat/page.tsx:421
- 分類: UX改善

### [P2] 知識マップ初期状態のノードが小さく、ラベルタップでは詳細を開けない
- 画面: /map
- 再現手順: 1. /mapを開き、構築完了を待つ → 2. 初期表示のノードまたはノード名をタップする → 3. 詳細シートが開くか確認する → 4. ズームインを3回行い、円形ノード自体をタップする
- 期待する挙動 / 実際の挙動: 期待は画面に見えているノード名をタップして詳細を開けること。実際は初期状態の円形ノードが約4×4pxで、表示ラベルをタップしても詳細が開かなかった。ズーム後に円形部分を直接タップすると詳細シートが開いた。ヒントには「ノードをタップで詳細」と表示されるため、見えているラベルと実際のタップ領域が一致していない。
- 該当コード（分かれば）: src/app/map/page.tsx:541、src/app/map/page.tsx:570
- 分類: UI崩れ

### [P3] 長時間の学習カード生成を中止できない
- 画面: /posts/[postId]/learning
- 再現手順: 1. 監査用投稿から確認画面へ進む → 2. 「学習カードを生成」をタップする → 3. 生成中の表示を確認する
- 期待する挙動 / 実際の挙動: 期待は、長時間処理をユーザーが中止できること。実際は「学習カードを生成しています…」と工程表示は出るが、キャンセル・中止ボタンは表示されなかった。今回の生成は約30秒で完了したため、途中停止そのものは実行していない。
- 該当コード（分かれば）: src/app/posts/[postId]/learning/page.tsx:842、src/app/posts/[postId]/learning/page.tsx:898
- 分類: UX改善

### [P3] 学習カード保存後も保存ボタンが同じ状態に戻る
- 画面: /posts/[postId]/learning
- 再現手順: 1. 学習カードを生成する → 2. 「学習カードに保存」をタップする → 3. 完了メッセージとボタン表示を確認する
- 期待する挙動 / 実際の挙動: 期待は「保存済み」表示またはボタン無効化など、保存完了状態が明確になること。実際は「学習カードに保存しました」というメッセージは表示されたが、ボタンのラベルは再び「学習カードに保存」となった。
- 該当コード（分かれば）: src/app/posts/[postId]/learning/page.tsx:498、src/app/posts/[postId]/learning/page.tsx:690
- 分類: UX改善

### [P3] 右上メニューのタップ領域が44px未満
- 画面: /（ヘッダー共通）
- 再現手順: 1. 375×812でホームを開く → 2. 右上のメニューアイコンの矩形サイズを確認する → 3. タップして開閉する
- 期待する挙動 / 実際の挙動: 期待は目安44×44px程度のタップ領域。実際のメニューボタンは約32×32pxだった。操作自体はタップで成立したが、片手操作時に押しにくいサイズである。
- 該当コード（分かれば）: src/components/layout/MobileNav.tsx:118
- 分類: UX改善

### [P3] 知識マップのズームボタンが44px未満
- 画面: /map
- 再現手順: 1. /mapを開く → 2. 右下の拡大・縮小ボタンのサイズを確認する → 3. タップしてズームする
- 期待する挙動 / 実際の挙動: 期待は目安44×44px程度のタップ領域。実際のズームボタンは36×36pxだった。操作自体は成立したが、ノードが小さい画面でズーム操作を頻繁に行う前提としては押しにくい。
- 該当コード（分かれば）: src/app/map/page.tsx:584
- 分類: UX改善

### [P3] チャット送信ボタンがアイコンのみで、アクセシブルな名前がない
- 画面: /chat
- 再現手順: 1. /chatを開く → 2. 入力欄の右端にある送信ボタンを確認する → 3. DOMスナップショットでボタン名を確認する
- 期待する挙動 / 実際の挙動: 期待はアイコンに「送信」などの名前があり、視覚・支援技術の両方で用途が分かること。実際はアイコンのみで、DOMスナップショット上も [disabled] の名前なしボタンだった。提案質問をタップして送信するフローは完了した。
- 該当コード（分かれば）: src/app/chat/page.tsx:526
- 分類: UX改善

## 良かった点

- /posts/new の空送信では「投稿本文を入力してください」というインラインバリデーションが表示された。
- 監査用の長文・改行入り投稿は保存でき、保存中は「保存中…」表示とボタン無効化が行われた。
- 学習カード生成中は工程表示があり、完了後に「学習カードを作成しました」と明示された。
- チャットは処理中に「考え中…」「送信中」表示となり、送信ボタンも無効化された。提案質問からの1回の質問は正常完了した。
- 資産庫では監査用に作成した資産をアーカイブし、「アーカイブを表示」から「戻す」で復元できた。

## 画面別の確認状況

375×812で実機操作した画面: /、/posts、/posts/new、監査用投稿の /posts/[postId]/confirm、/posts/[postId]/learning、/chat、/map、/assets。ホーム、保存した投稿、投稿追加、確認、学習、AIチャット、知識マップ、資産庫では、確認できた範囲でコンソールエラーおよびネットワークの4xx/5xxは発生しなかった。

未確認（サーバー応答停止後）: /knowhow、/collections、/collections/[collectionId]、/insights、/drafts、/settings、/settings/x、/settings/bookmarklet、存在しないURL /posts/does-not-exist、768pxでの主要画面確認。これらは推測で問題件数に含めていない。

監査用に作成した投稿は、長文・改行入力、学習カード生成、学習カード保存、資産庫でのアーカイブと復元に使用した。既存データの削除・編集は行っていない。AI生成は学習カード生成1回、チャット質問1回に限定した。

## 修正対応（2026-07-10）

### 1. [P2] ドロワー背面スクロール
対応内容: body固定（scrollY保存・復元、position/top/width/overflow）とオーバーレイの overscroll-contain touch-none を追加。
変更ファイル: src/components/layout/MobileNav.tsx。検証方法と結果: tsc+コードレビューで固定・解除・位置復元を確認（localhost:3000未応答のため実機未実施）。

### 2. [P2] セグメントラベル表示
対応内容: モバイルでラベルを隠す分岐を削除し、横スクロール・各項目shrink-0・スクロールバー非表示・aria-labelを追加。
変更ファイル: src/components/ui/SegmentedControl.tsx、src/app/posts/new/page.tsx、src/app/chat/page.tsx。検証方法と結果: tsc+コードレビューで5種別/4モードの常時ラベル表示を確認（実機未実施）。

### 3. [P2] 知識マップのノード操作
対応内容: 画面半径22px相当の透明ヒットエリアとノード名ラベルのクリックハンドラを追加し、既存ズーム操作を維持。
変更ファイル: src/app/map/page.tsx。検証方法と結果: tsc+コードレビューで初期ズームのタップ経路を確認（実機未実施）。

### 4. [P3] 学習カード生成の中止
対応内容: 生成fetchにAbortControllerを紐付け、「中止する」ボタンとAbortError専用の「生成を中止しました」表示を追加。
変更ファイル: src/app/posts/[postId]/learning/page.tsx。検証方法と結果: tsc+コードレビューで通常エラーとabortの分離、生成前表示への復帰を確認（AI実機確認未実施）。

### 5. [P3] 学習カード保存済み表示
対応内容: 保存成功後は「保存済み ✓」表示かつdisabledにし、再生成時はAPIのdraft状態で保存ボタンへ戻るようにした。
変更ファイル: src/app/posts/[postId]/learning/page.tsx。検証方法と結果: tsc+コードレビューで保存済み状態と二重保存防止を確認（AI実機確認未実施）。

### 6. [P3] MobileNavのタップ領域
対応内容: 開く/閉じるボタンをmin-h-11/min-w-11・中央寄せへ変更し、ネガティブマージンでヘッダー見た目を維持。
変更ファイル: src/components/layout/MobileNav.tsx。検証方法と結果: tsc+コードレビューで両ボタンの44px以上のクラスを確認（実機未実施）。

### 7. [P3] 知識マップのズームボタン
対応内容: 拡大・縮小・全体表示の3ボタンをh-11/w-11へ変更し、既存のgap-1.5を維持。
変更ファイル: src/app/map/page.tsx。検証方法と結果: tsc+コードレビューで44pxサイズとボタン間隔を確認（実機未実施）。

### 8. [P3] チャット送信ボタンの名前
対応内容: 送信ボタンにaria-label="送信"を追加し、chat内の検索履歴削除アイコンにもaria-label="削除"を追加。
変更ファイル: src/app/chat/page.tsx。検証方法と結果: tsc+コードレビューでchat/newのアイコンのみ操作を確認（localhost:3000未応答のため実機未実施）。

## 追補監査（2026-07-11・実施済み）

実施方法: 監査担当モデルの環境でビューポート変更とスクリーンショットが機能しなかったため、Claude Code本体が実施。ブラウザのウィンドウリサイズが効かない環境だったため、同一オリジンページ内に375×812（後半768×1024）のiframeを作ってアプリを読み込み、実レイアウト・実操作で確認した（CSSメディアクエリはiframe幅に正しく反応する）。AI生成は0回。作成したテストデータ（コレクション1件）は確認後に削除済み。

### サマリ

- Part 1（8修正のスポットチェック）: **6項目すべて合格**（「生成中止」はコードレビュー済みのため対象外）。
- Part 2（未監査画面+768px）: 新規問題 **2件**（P2: 1件 → その場で修正済み / P3: 1件）。
- コンソールエラー: アプリ起因のものは0件。検出された42件はすべてiframeの高速ページ切替に起因するfetch中断・アンマウント中断のアーティファクト（タイムスタンプがiframe遷移と一致）。

### Part 1: 修正のスポットチェック（375×812）

| 項目 | 判定 | 根拠 |
| --- | --- | --- |
| 1. ドロワーの背面固定 | 合格 | scrollY=300で開くとbodyがposition:fixed/top:-300pxになり、scrollByが無効（scrollY=0のまま）。閉じるとstyleが復元されscrollY=300に戻った。 |
| 2. メニューボタンのタップ領域 | 合格 | 開くボタン44×44px、ドロワー内閉じるボタン44×44px。 |
| 3. タブのラベル表示 | 合格 | /posts/newの5種別・/chatの4モード・/postsの2タブすべてラベル表示+aria-labelあり。/posts/newはコンテナ横スクロール可（scrollWidth 395 > clientWidth 326）。全画面でページ横はみ出しなし。 |
| 4. 知識マップ | 合格 | 初期ズームのままノード名ラベル（svg text）クリックで詳細シートが開いた。拡大・縮小・全体表示ボタンすべて44×44px+aria-labelあり。 |
| 5. 保存済み表示 | 合格 | 保存済みカードの学習ページでボタンが「保存済み ✓」表示かつdisabled。生成・作り直しは押していない。 |
| 6. 送信ボタンの名前 | 合格 | /chatの送信ボタンにaria-label="送信"あり（入力空時はdisabled）。 |

### Part 2: 追補監査の結果

確認済み画面（375×812）: /knowhow、/collections、/collections/[collectionId]（テスト用コレクションで確認）、/insights、/drafts、/settings（AI設定アコーディオン展開含む）、/settings/x、/settings/bookmarklet、/posts/does-not-exist、/totally-unknown-page。
確認済み画面（768×1024）: /、/posts、/posts/new、/chat、/assets、/knowhow、/collections、/insights、/drafts、/settings、/map。

#### [P2] 768px幅でホームの掛け合わせカードのボタン行が横はみ出し【修正済み】
- 画面: /（768×1024）
- 再現手順: 1. 768px幅でホームを開く → 2. 今日の掛け合わせカードのボタン行を確認
- 期待/実際: 期待はボタンが収まるか折り返すこと。実際は「今日はスキップ」ボタンがright=847pxまではみ出し、ページ全体に横スクロール（scrollWidth 847）が発生。sm:flex-rowの行に3ボタン+whitespace-nowrapで折り返し不可だったため。
- 該当コード: src/components/synthesis/TodaySynthesisCard.tsx:236
- 対応: `sm:flex-wrap` を追加して修正済み。修正後 scrollWidth 753 ≤ 768 を実機確認。tsc エラー0。

#### [P3] /postsのソートセレクトの選択中ラベルが375pxで途切れる
- 画面: /posts（375×812）
- 再現手順: 1. 375px幅で/postsを開く → 2. フィルタ行のソートセレクトを確認
- 期待/実際: 期待は選択中ラベルが読めること。実際は「いいねした日が新しい順」が幅160pxのselectで途中で切れる（select.scrollWidth > clientWidth）。タップすればOSのピッカーで全選択肢は読めるため低優先。
- 分類: UI崩れ（軽微）
- 対応案: ソートだけ全幅にする、または短いラベル（「新しい順」等）に変える。

#### 誤検知として棄却したもの（記録）
- /settings/x で画面外に見えた「1件追加」→ `max-h-72 overflow-y-auto` の内部スクロール領域内で正常に到達可能。
- /collections で「カード0件のまま作成を押しても無反応」→ 実際は「学習カードを1つ以上選んでください」ダイアログが正しく表示される（検出スクリプトの文言マッチ漏れ）。
- 画面左下のボトムタブに重なる円形要素 → Next.js開発モードのインジケーター（NEXTJS-PORTAL）。本番には出ない。

### 良かった点（追補）

- 404は未知URL・実在しない投稿IDの両方で「ページが見つかりません+ホームへ戻る」を表示。/posts/undefined/learning でも「投稿が見つかりません」と安全に処理。
- /collections は0件時に空状態の説明+「新規作成」+おすすめ提案を表示。作成フォームのバリデーション（タイトル必須・カード1件以上）はダイアログで明示。
- コレクション削除は確認ダイアログ（danger表示の「削除する」）→一覧へ戻る、と一貫した流れ。
- 全確認画面でボトムタブによるコンテンツ隠れなし、375px横はみ出しなし。768pxでサイドバー/モバイルナビの切替も正常。
